import { Box } from "@mui/system";

export default function Test() {
  return (
    <Box
      sx={{
        width: "100%",
        height: "100%",
        display: "flex",
        flexDirection: "row",
        // backgroundColor: "#1f1f1f",
      }}
    >
      {/* Image start */}
    </Box>
  );
}
